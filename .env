# AWS Configuration for VSM App Deployment
# AWS credentials and region are read from ~/.aws/credentials and ~/.aws/config
# Only configure the variables below

# AWS Profile to use (default: default)
# If you have multiple AWS profiles, specify which one to use here
AWS_PROFILE=vsmapp

# S3 Bucket Name for hosting the static site
S3_BUCKET_NAME=vsmapp-production

# Application name/environment
APP_NAME=vsmapp
ENVIRONMENT=production
